= simple_form_for @personal_info, :url => user_personal_info_path(current_user), :html => {:multipart => true}  do |f|
  - if @personal_info.errors.any?
    #error_explanation
      %h2= "#{pluralize(@personal_info.errors.count, "error")} prohibited this personal_info from being saved:"
      %ul
        - @personal_info.errors.full_messages.each do |msg|
          %li= msg

  .field 
    = f.label "Upload an image"
    = f.input :image
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :surname
    = f.text_field :surname
  .field
    = f.label :age
    = f.number_field :age
  .field
    = f.label :country
    = f.text_field :country
    = f.label :about
    = f.text_field :about
  .actions
    = f.submit 'Save'
